#include<stdio.h>

int main(){
	 
	 int i=10; 
	 int j=i;
	 int a=2, b=3, c=4, d=5;
	 
	 // %d is called format specifier
	 // %d for int , %f for float and %c for characters
	 
	 printf("the value of i is %d and value of j is %d\n ",i , j);
	 printf("the value of a is %d and value of b is %d\n", a, b);
	 printf("the value of c is %d and value of d is %d\n",c, d);
   
	
	return 0;
}
/* intruction to declare a variable 
1.int for integer values 
2.float for floating integers(decimals)
3.char for characters
NOTE--> if humne ek baari koi variable define kr diya h to hum usse wapis use nhi kr skte 
e.g int a;
      float a;    (ye situation invalid h coz yaha "a" variable 2 baari define hua h)
*/