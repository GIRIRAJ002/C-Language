#include<stdio.h>

int main(){
    int g;
    scanf("%d", &g);
    printf("the value of g is %d", g);
    return 0;
}
// scan f function basically user ko allow krta h to input values in the function and then execute it .
